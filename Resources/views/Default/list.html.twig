{% extends '::base.html.twig' %}

{% block body %}
    <h1>{{ section }}</h1>

    {% for paper in papers %}
        <div><a href="{{ url('anh_content_' ~ section ~ '_view', { 'section': section, 'slug': paper.getSlug() }) }}">{{ paper.getTitle() }}</a></div>
    {% endfor %}
{% endblock %}
